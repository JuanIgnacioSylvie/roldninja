# Base de datos (coincide con docker-compose.yml)
DATABASE_URL="postgresql://roldninja:roldninja@localhost:5432/roldninja?schema=public"

# Auth
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"

# Servidor (Fastify + Socket.IO)
PORT=4000

# Frontend (Next.js)
NEXT_PUBLIC_SERVER_URL="http://localhost:4000"
NEXT_PUBLIC_AZGAAR_URL="/azgaar/index.html"
